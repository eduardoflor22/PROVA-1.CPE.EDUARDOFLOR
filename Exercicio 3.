Eduardo Flor Ocampo - 242014328
EXERCICIO 3: CAMINHO DA FORMIGUINHA

#include <iostream>
using namespace std;

int main() {
    int T, N;
    
    //Casos para o problema:
    cin >> T; 

if (T<=100 && T<=109 && T>=1){
     while (T--) {

     // Tamanho do tronco
    cin >> N;

    // Tempo máximo
    cout << N / 2 << endl;
    
    }

}
else{
    cout << "Numeros fora do intervalo permitido pelo exercicio" << endl;
}
    return 0;
}
